"use client";
import { cn } from "@/lib/utils";

const ChatInfo = ({ className }: { className?: string }) => {
  return <div className={cn(className)}>Chat Info (coming soon...)</div>;
};

export default ChatInfo;
